import { BuscarPuestoPipe } from './buscar-puesto.pipe';

describe('BuscarPuestoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPuestoPipe();
    expect(pipe).toBeTruthy();
  });
});
