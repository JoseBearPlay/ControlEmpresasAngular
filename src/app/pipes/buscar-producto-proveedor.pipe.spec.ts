import { BuscarProductoProveedorPipe } from './buscar-producto-proveedor.pipe';

describe('BuscarProductoProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarProductoProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
