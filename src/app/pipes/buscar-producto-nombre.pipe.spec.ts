import { BuscarProductoNombrePipe } from './buscar-producto-nombre.pipe';

describe('BuscarProductoNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarProductoNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
